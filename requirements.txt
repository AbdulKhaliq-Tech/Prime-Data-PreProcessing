Flask==3.0.3
python-dotenv==1.0.1
pytest==8.3.3

# Libraries named in the SRS software requirements, pinned now so later
# phases (Import, Cleaning, Statistical Analysis, Visualization, File
# Conversion, Word Report Generator, Mock Data Generation) can rely on them
# without re-negotiating dependencies. Not used by Phase 00 itself.
pandas==2.2.3
numpy==2.1.2
matplotlib==3.9.2
plotly==5.24.1
openpyxl==3.1.5
python-docx==1.1.2
requests==2.32.3
Faker==30.3.0
